apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: coenbreg/saddit-react-frontend:feature-azure
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
